// SKILL: MOBILE_APP_EXPERIENCE
// TECH: TAILWIND + LUCIDE REACT

// 1. Floating Action Suite (WhatsApp & Call)
// Should only appear on mobile (md:hidden)
const FloatingActions = () => (
  <div className="fixed bottom-6 right-6 flex flex-col gap-4 z-50 md:hidden animate-bounce-subtle">
    <a href="tel:+212..." className="p-4 bg-white text-primary rounded-full shadow-xl border border-outline-variant">
       <PhoneIcon size={24} />
    </a>
    <a href="https://wa.me/212..." className="p-4 bg-primary text-white rounded-full shadow-xl">
       <WhatsAppIcon size={24} />
    </a>
  </div>
);

// 2. PWA Manifest (Conceptual)
// Ensure next-pwa is configured in next.config.js for "Add to Home Screen" feature.