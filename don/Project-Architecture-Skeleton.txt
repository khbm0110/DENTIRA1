// SKILL: PROJECT_ARCHITECTURE_SKELETON
// ROLE: DIRECTORY STRUCTURE & COMPONENT HIERARCHY
// STRICT RULE: MAINTAIN 100/100 LIGHTHOUSE PERFORMANCE

1. DIRECTORY STRUCTURE:
/src
  /app
    /[lang]            // i18n Route Segment (fr/ar)
      /admin           // Protected Admin Dashboard
      /login           // Supabase Auth Page
      /layout.tsx      // Global Layout (PWA & SEO Meta)
      /page.tsx        // Hero, Services, Reviews, Contact (Single Page Style)
  /components
    /ui                // Atomic Components (Buttons, Inputs)
    /shared            // Navbar, Footer, FloatingActions
    /sections          // HeroSection, ServicesSection, BookingForm
  /config
    /dentora-system.ts // The Source of Truth (Linked to System-Core-Config)
  /lib
    /supabase          // Supabase Client & Database Methods
    /i18n              // Middleware & Translation Logic
  /public
    /icons             // PWA Manifest Icons
    /images            // Optimized WebP/Avif Clinic Photos

2. COMPONENT LOGIC:
- Use Server Components by default for SEO & Speed.
- Use Client Components ('use client') ONLY for Interactive UI (Forms, Floating Buttons).
- Implementation of "Android-App-Feel":
    * Fixed Header with Blur Effect.
    * Bottom Navigation Bar (Mobile Only).
    * Pull-to-refresh logic (Optional/PWA).

3. DATABASE SCHEMA (Supabase):
- Table: 'appointments' (id, name, phone, service, status, created_at).
- Table: 'settings' (Dynamic overrides for ClinicConfig if needed).

4. SEO STRATEGY:
- Dynamic Metadata Generation based on [lang] segment.
- JSON-LD Schema (MedicalBusiness) injection for Google Rich Results.
- Sitemap.xml & Robots.txt generation.

5. THE "DENTORA" SPECIAL:
- WhatsApp Redirection Logic: Constructing URL strings with professional FR/AR templates.
- Google Reviews Fetcher: Server-side caching of Places API results to avoid rate limits.